Use the streamlit_to_react skill(copy it to respective .llm_tool/skills folder) to convert any streamlit app into a functional full stack app(Python + TS/React). Contains a sample streamlit app which was migrated to a fullstack functioning app using gemini cli.